export default (v => {
  const { x, b } = v;

  [].forEach.call(b, e => {
    // v.f = e.getAttribute('hex');
    e.addEventListener('click', e => { x.loadpageu({ u: '/want/tangent' }) });
  });

  const devu = () => '//\ v0.0.240923';
  v.devu = devu;

  return v;
})({ x: hex, b: document.querySelectorAll('.btn') });